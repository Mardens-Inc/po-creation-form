info:
  name: Get SubCategory
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/data/subcategory/:category_id"
  params:
    - name: category_id
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
