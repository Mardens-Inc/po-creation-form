info:
  name: Enable MFA
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/auth/mfa/enable"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
