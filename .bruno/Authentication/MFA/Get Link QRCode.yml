info:
  name: Get Link QRCode
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/auth/mfa/link-qrcode.svg"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
