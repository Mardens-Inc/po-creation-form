info:
  name: Delete Vendor
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/vendors/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
