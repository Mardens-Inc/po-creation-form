info:
  name: Upload File
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/purchase-orders/:po_id/files?filename=test.pdf&asset_type=0"
  params:
    - name: po_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
