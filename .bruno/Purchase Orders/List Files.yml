info:
  name: List Files
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/purchase-orders/:po_id/files"
  params:
    - name: po_id
      value: ""
      type: path
    auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
