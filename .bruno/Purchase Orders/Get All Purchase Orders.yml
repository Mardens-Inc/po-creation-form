info:
  name: Get All Purchase Orders
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/purchase-orders"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
