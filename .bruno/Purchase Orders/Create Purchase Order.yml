info:
  name: Create Purchase Order
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/purchase-orders"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
