info:
  name: Get Purchase Order
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/purchase-orders/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
