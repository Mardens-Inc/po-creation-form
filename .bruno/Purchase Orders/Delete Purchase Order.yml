info:
  name: Delete Purchase Order
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/purchase-orders/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
