info:
  name: Delete File
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base_url}}/purchase-orders/:po_id/files/:file_id"
  params:
    - name: po_id
      value: ""
      type: path
    - name: file_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
