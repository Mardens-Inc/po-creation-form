info:
  name: Get Category
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/data/category/:department_id"
  params:
    - name: department_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
