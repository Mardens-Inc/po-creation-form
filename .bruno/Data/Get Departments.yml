info:
  name: Get Departments
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/data/department"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
